---
---

<section id="skills" class="py-20 px-4 bg-muted/30">
  <div class="max-w-6xl mx-auto">
    <h2 class="text-3xl md:text-4xl font-bold text-center mb-12">
      Skills & Technologies
    </h2>
    
    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
      {/* Frontend Development */}
      <div class="hover:shadow-lg transition-all duration-300 hover:-translate-y-1 rounded-lg border bg-card text-card-foreground shadow-sm">
        <div class="flex flex-col space-y-1.5 p-6">
          <h3 class="text-2xl font-semibold leading-none tracking-tight flex items-center gap-2">
            <span class="text-2xl">🎨</span>
            Frontend Development
          </h3>
        </div>
        <div class="p-6 pt-0">
          <div class="flex flex-wrap gap-2">
            {/* TODO: Update with your actual frontend skills */}
            {['React', 'Vue.js', 'Next.js', 'TypeScript', 'Tailwind CSS', 'HTML5', 'CSS3', 'JavaScript', 'Responsive Design'].map(skill => (
              <span class="inline-flex items-center rounded-full border px-2.5 py-0.5 text-xs font-semibold transition-colors border-transparent bg-secondary text-secondary-foreground hover:bg-secondary/80">
                {skill}
              </span>
            ))}
          </div>
        </div>
      </div>
      
      {/* Backend Development */}
      <div class="hover:shadow-lg transition-all duration-300 hover:-translate-y-1 rounded-lg border bg-card text-card-foreground shadow-sm">
        <div class="flex flex-col space-y-1.5 p-6">
          <h3 class="text-2xl font-semibold leading-none tracking-tight flex items-center gap-2">
            <span class="text-2xl">⚙️</span>
            Backend Development
          </h3>
        </div>
        <div class="p-6 pt-0">
          <div class="flex flex-wrap gap-2">
            {/* TODO: Update with your actual backend skills */}
            {['Node.js', 'Express', 'Python', 'Django', 'PostgreSQL', 'MongoDB', 'Redis', 'REST APIs', 'GraphQL'].map(skill => (
              <span class="inline-flex items-center rounded-full border px-2.5 py-0.5 text-xs font-semibold transition-colors border-transparent bg-secondary text-secondary-foreground hover:bg-secondary/80">
                {skill}
              </span>
            ))}
          </div>
        </div>
      </div>
      
      {/* Tools & DevOps */}
      <div class="hover:shadow-lg transition-all duration-300 hover:-translate-y-1 rounded-lg border bg-card text-card-foreground shadow-sm">
        <div class="flex flex-col space-y-1.5 p-6">
          <h3 class="text-2xl font-semibold leading-none tracking-tight flex items-center gap-2">
            <span class="text-2xl">🛠️</span>
            Tools & DevOps
          </h3>
        </div>
        <div class="p-6 pt-0">
          <div class="flex flex-wrap gap-2">
            {/* TODO: Update with your actual tools/devops skills */}
            {['Git', 'Docker', 'AWS', 'CI/CD', 'Linux', 'Nginx', 'Webpack', 'Vite', 'Jest'].map(skill => (
              <span class="inline-flex items-center rounded-full border px-2.5 py-0.5 text-xs font-semibold transition-colors border-transparent bg-secondary text-secondary-foreground hover:bg-secondary/80">
                {skill}
              </span>
            ))}
          </div>
        </div>
      </div>
      
      {/* Web Design */}
      <div class="hover:shadow-lg transition-all duration-300 hover:-translate-y-1 rounded-lg border bg-card text-card-foreground shadow-sm">
        <div class="flex flex-col space-y-1.5 p-6">
          <h3 class="text-2xl font-semibold leading-none tracking-tight flex items-center gap-2">
            <span class="text-2xl">🎯</span>
            Web Design
          </h3>
        </div>
        <div class="p-6 pt-0">
          <div class="flex flex-wrap gap-2">
            {/* TODO: Update with your actual design skills */}
            {['Figma', 'Adobe XD', 'UI/UX Design', 'Wireframing', 'Prototyping', 'Design Systems', 'Accessibility'].map(skill => (
              <span class="inline-flex items-center rounded-full border px-2.5 py-0.5 text-xs font-semibold transition-colors border-transparent bg-secondary text-secondary-foreground hover:bg-secondary/80">
                {skill}
              </span>
            ))}
          </div>
        </div>
      </div>
      
      {/* Marketing */}
      <div class="hover:shadow-lg transition-all duration-300 hover:-translate-y-1 rounded-lg border bg-card text-card-foreground shadow-sm">
        <div class="flex flex-col space-y-1.5 p-6">
          <h3 class="text-2xl font-semibold leading-none tracking-tight flex items-center gap-2">
            <span class="text-2xl">📈</span>
            Digital Marketing
          </h3>
        </div>
        <div class="p-6 pt-0">
          <div class="flex flex-wrap gap-2">
            {/* TODO: Update with your actual marketing skills */}
            {['SEO', 'Content Strategy', 'Google Analytics', 'Social Media', 'Email Marketing', 'A/B Testing', 'Copywriting'].map(skill => (
              <span class="inline-flex items-center rounded-full border px-2.5 py-0.5 text-xs font-semibold transition-colors border-transparent bg-secondary text-secondary-foreground hover:bg-secondary/80">
                {skill}
              </span>
            ))}
          </div>
        </div>
      </div>
      
      {/* Soft Skills */}
      <div class="hover:shadow-lg transition-all duration-300 hover:-translate-y-1 rounded-lg border bg-card text-card-foreground shadow-sm">
        <div class="flex flex-col space-y-1.5 p-6">
          <h3 class="text-2xl font-semibold leading-none tracking-tight flex items-center gap-2">
            <span class="text-2xl">🤝</span>
            Soft Skills
          </h3>
        </div>
        <div class="p-6 pt-0">
          <div class="flex flex-wrap gap-2">
            {/* TODO: Update with your actual soft skills */}
            {['Problem Solving', 'Team Collaboration', 'Communication', 'Project Management', 'Agile/Scrum', 'Leadership', 'Mentoring'].map(skill => (
              <span class="inline-flex items-center rounded-full border px-2.5 py-0.5 text-xs font-semibold transition-colors border-transparent bg-secondary text-secondary-foreground hover:bg-secondary/80">
                {skill}
              </span>
            ))}
          </div>
        </div>
      </div>
    </div>
  </div>
</section>